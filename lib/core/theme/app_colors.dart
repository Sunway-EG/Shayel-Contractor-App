import 'package:flutter/cupertino.dart';

/// Sun-Way Truck UI design system colors.
abstract final class AppColors {
  /// Light gray (0) - backgrounds, borders.
  static const Color lightGray = Color(0xFFDAE2E7);

  /// Medium blue-gray (25).
  static const Color mediumBlueGray = Color(0xFFACC2CD);

  /// Vibrant blue (100) - accents.
  static const Color vibrantBlue = Color(0xFF3B94C0);

  /// Main / dark blue - primary actions, headers.
  static const Color mainBlue = Color(0xFF005BAA);

  /// Main gray - backgrounds (25).
  static const Color mainGray = Color(0xF6F6F6F6);

  /// White (for text on main blue).
  static const Color white = Color(0xFFFFFFFF);

  /// Dark gray for body text on light backgrounds.
  static const Color darkGray = Color(0xFF333333);

  /// Light teal/mint for selected/highlighted cards.
  static const Color lightMint = Color(0xFFE8F4F8);

  /// Green color for success/online status.
  static const Color green = Color(0xFF4CAF50);

  /// New green color for new design.
  static const Color newgreen = Color(0xFF2EA154);

  /// Red color for error/offline status.
  static const Color red = Color(0xFFE53935);

  static const Color orange = Color(0xFFFFBC1F);
}
